---
  - name: Initial setup
    hosts: all
    roles: 
        - init

  - name: Database servers
    hosts: db_servers
    roles:
      - mysql

  - name: Web servers
    hosts: web_servers
    roles:
      - agama
      - uwsgi
      - nginx
